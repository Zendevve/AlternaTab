---
phase: 05-first-run-onboarding-accessibility
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/background/index.ts
  - src/content/index.ts
  - src/onboarding/index.html
  - src/onboarding/index.css
  - src/onboarding/index.ts
  - src/onboarding/__tests__/index.test.ts
autonomous: true
requirements:
  - NFR-005
must_haves:
  truths:
    - "First installation opens a beautiful, premium dedicated onboarding tab page"
    - "Onboarding contains a 3-step walkthrough with configuration toggles and interactive switcher sandbox"
    - "Switcher overlay DOM elements have proper ARIA roles and visible outline indicators, exceeding WCAG 2.2 accessibility guidelines"
  artifacts:
    - path: "src/onboarding/index.html"
      provides: "First-run welcome wizard structure with card wizard controls"
    - path: "src/onboarding/index.css"
      provides: "Sleek glassmorphic styles matching options dashboard"
    - path: "src/onboarding/index.ts"
      provides: "Wizard controller handling slide state, settings toggling, and keyboard mock switcher"
    - path: "src/onboarding/__tests__/index.test.ts"
      provides: "Unit tests checking onboarding state and mock keyboard event handler"
  key_links:
    - from: "src/background/index.ts"
      to: "src/onboarding/index.html"
      via: "chrome.runtime.onInstalled chrome.tabs.create"
      pattern: "chrome.tabs.create.*"
---

<objective>
Implement first-install onboarding redirection to a stunning 3-step glassmorphic walkthrough dashboard featuring a live simulated in-page tab switcher sandbox, and perform an audit on the content switcher DOM overlays to introduce ARIA tags and focus indicators to fully exceed WCAG 2.2 requirements.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-first-run-onboarding-accessibility/05-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Onboarding Page & Automatic Install Launch</name>
  <files>src/background/index.ts, src/onboarding/index.html, src/onboarding/index.css</files>
  <action>
    Configure background service worker to launch onboarding on install:
    - Add `chrome.runtime.onInstalled.addListener` to `src/background/index.ts`.
    - If `details.reason === "install"`, call `chrome.tabs.create` pointing to `src/onboarding/index.html` (resolves relative path cleanly).
    
    Scaffold the onboarding HTML structure in `src/onboarding/index.html`:
    - Layout a centered welcome dashboard styled with premium Outfit/Inter typography.
    - Create a 3-step navigation wizard deck.
    
    Add CSS variables and basic glassmorphic card elements in `src/onboarding/index.css`:
    - Align colors with options page HSL variables.
    - Implement slide container offsets, subtle blur background effects, animations, buttons, and custom layout settings.
  </action>
  <verify>npx tsc --noEmit</verify>
  <acceptance_criteria>
    - background worker registers chrome.runtime.onInstalled
    - src/onboarding/index.html exists and incorporates index.css style sheet reference
    - Code builds successfully without type compilation issues
  </acceptance_criteria>
  <done>Onboarding layout files are scaffolded and install hook wired up</done>
</task>

<task type="auto">
  <name>Task 2: Build Interactive Welcome Wizard & Switcher Playground</name>
  <files>src/onboarding/index.ts</files>
  <action>
    Create src/onboarding/index.ts implementing the multi-step welcome wizard logic:
    - Implement Step 1: Mode Selection (Hold vs Toggle mode). Read storage defaults, bind change listeners, save choice using `StorageManager.saveSettings`.
    - Implement Step 2: Interactive Alt+Q Playground. Renders a mock tab list overlay in-page. Guide instructs user: "Press Alt+Q (or Click the cards) to cycle tabs in the playground below".
      - Write an in-page mock KeyHandler that listens to events within the sandbox element, cycling selection highlights forward/backward on Alt+Q/Alt+Shift+Q and triggering mock confirmations on Enter or key releases.
    - Implement Step 3: Global shortcuts activation instructions. Display hotkeys list and include a direct button link to open `chrome://extensions/shortcuts` inside Chrome via `chrome.tabs.create`.
    - Implement NEXT/PREV control handlers for moving between wizard step slide indicators.
  </action>
  <verify>npx tsc --noEmit</verify>
  <acceptance_criteria>
    - Onboarding steps toggle visibility based on active step index
    - In-page mock playground captures and cycles selections cleanly
    - Direct chrome shortcuts button calls tab creator API
  </acceptance_criteria>
  <done>Welcome wizard slide navigation and mock playground switching completed</done>
</task>

<task type="auto">
  <name>Task 3: Switcher Overlay Accessibility Audit & Enhancements</name>
  <files>src/content/index.ts</files>
  <action>
    Enhance accessibility (WCAG 2.2) of the core switcher overlay in `src/content/index.ts`:
    - List Container element: add `role="listbox"`, `aria-label="Recent Tabs Switcher"`, and `tabindex="0"`.
    - Card elements: add `role="option"`, `aria-selected="true|false"` matching the highlight, and `id` for active description mapping.
    - Set `aria-activedescendant` on the list container to point to the active card's element ID, so screen readers announce tab names correctly when active cycling occurs.
    - Ensure outline/focus indicators are present and readable.
  </action>
  <verify>npx tsc --noEmit</verify>
  <acceptance_criteria>
    - Switcher overlay containers have appropriate ARIA listbox roles
    - Cards have role="option" and aria-selected mirroring active highlight
    - Focus outline styles present and compile successfully
  </acceptance_criteria>
  <done>Accessibility improvements implemented in content overlay script</done>
</task>

<task type="auto">
  <name>Task 4: Unit Testing Onboarding & Accessibility</name>
  <files>src/onboarding/__tests__/index.test.ts</files>
  <action>
    Create src/onboarding/__tests__/index.test.ts utilizing Vitest and JSDOM context:
    - Verify that wizard step elements exist and slide toggle updates correctly.
    - Mock chrome APIs and verify that changing hold/toggle inputs saves settings in storage sync.
    - Test simulated Alt+Q playground cycling inside the mock in-page DOM.
    - Assert that ARIA tags (role="listbox", role="option") are set properly on the rendered content overlay elements.
  </action>
  <verify>npx vitest run src/onboarding/__tests__/index.test.ts</verify>
  <acceptance_criteria>
    - Onboarding unit tests pass cleanly with 100% success
  </acceptance_criteria>
  <done>Complete unit test coverage for onboarding wizard and accessibility tags written</done>
</task>

</tasks>

<verification>
- [ ] ts/tsc --noEmit compiles cleanly without warnings
- [ ] npx vitest run src/onboarding/__tests__/index.test.ts passes completely
- [ ] all project unit tests run successfully: npx vitest run
</verification>

<success_criteria>
- Onboarding dashboard is fully responsive, glassmorphic, and guides first-time users perfectly
- Live in-page simulated sandbox playground provides clear cycling visual cues
- Custom active descendant and ARIA states allow complete compatibility with screen readers
</success_criteria>

<output>
After completion, create `.planning/phases/05-first-run-onboarding-accessibility/05-01-SUMMARY.md`
</output>
