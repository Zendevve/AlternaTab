---
phase: 04-settings-ui-quick-action-popup
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/options/index.html
  - src/options/index.css
  - src/options/index.ts
  - src/popup/index.html
  - src/popup/index.css
  - src/popup/index.ts
  - src/options/__tests__/index.test.ts
  - src/popup/__tests__/index.test.ts
autonomous: true
requirements:
  - FR-007
  - FR-008
must_haves:
  truths:
    - "Options panel saves and restores UserSettings sync parameters"
    - "Toolbar popup reads MRU metrics and redirects keyboard commands shortcuts setup"
    - "TypeScript compiles flawlessly with premium styling and animations"
  artifacts:
    - path: "src/options/index.ts"
      provides: "Options dashboard sync state management script"
    - path: "src/popup/index.ts"
      provides: "Toolbar popover settings quick actions and shortcut launchers"
  key_links:
    - from: "src/options/__tests__/index.test.ts"
      to: "src/options/index.ts"
      via: "unit test imports"
      pattern: "import.*"
---

<objective>
Implement the fully custom glassmorphic Options Dashboard page and lightweight Action Popover page, fully styled in vanilla CSS, wired to local and sync StorageManagers, and verified via detailed DOM interaction unit tests.

Purpose: Preferences sync dashboard and shortcuts settings panel.
Output: Options html/css/ts files, Popup html/css/ts files, and comprehensive test suites.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-settings-ui-quick-action-popup/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Premium Options settings dashboard</name>
  <files>src/options/index.html;src/options/index.css;src/options/index.ts</files>
  <action>
    Create src/options/index.html with modern layout:
    - Heading "alternaTab Configuration"
    - Card panels for Max Tabs Visible (slider), Activation Mode (radio), Themes (select), and Layout Grid/List (radio).
    - Dynamic active styling states, reset buttons, and "Configure Shortcuts" links.
    
    Create src/options/index.css applying premium harmonized dark/light CSS parameters:
    - Modern Outfit/Inter font family import
    - Sleek gradients, glassmorphic inputs, transition highlights, active visual glows.
    
    Create src/options/index.ts:
    - Read settings from StorageManager.getSettings() and set matching DOM values on DOMContentLoaded.
    - Add change listeners to auto-save to StorageManager.saveSettings() upon every selection, showing visual "Settings saved successfully!" temporary toast notifications.
  </action>
  <verify>npx tsc --noEmit</verify>
  <acceptance_criteria>
    - Options dashboard compiles without TypeScript type errors
    - Settings save/load actions fully hook to StorageManager
  </acceptance_criteria>
  <done>Options dashboard complete and integrated</done>
</task>

<task type="auto">
  <name>Task 2: Build Lightweight Action popover panel</name>
  <files>src/popup/index.html;src/popup/index.css;src/popup/index.ts</files>
  <action>
    Create src/popup/index.html with a sleek, compact summary card.
    - Visual indicators showing active MRU status.
    - Quick action toggle settings (Layout: Grid vs List, Theme: Dark vs Light).
    - Premium "Open native shortcuts" button.
    
    Create src/popup/index.css:
    - Matching premium mini HSL layout with standard widths (320px max).
    
    Create src/popup/index.ts:
    - Fetch active settings and render visual status.
    - Wire shortcut setup click trigger to call `chrome.tabs.create({ url: "chrome://extensions/shortcuts" })`.
  </action>
  <verify>npx tsc --noEmit</verify>
  <acceptance_criteria>
    - Popup layout compiles cleanly
    - Shortcuts click opens native extensions settings
  </acceptance_criteria>
  <done>Toolbar popup complete and ready</done>
</task>

<task type="auto">
  <name>Task 3: Preferences & DOM Unit Tests</name>
  <files>src/options/__tests__/index.test.ts;src/popup/__tests__/index.test.ts</files>
  <action>
    Create src/options/__tests__/index.test.ts and src/popup/__tests__/index.test.ts:
    - Write unit tests using pure jsdom in-memory HTML tree simulations.
    - Assert that loading the Options page populates form inputs with DEFAULT_SETTINGS values accurately.
    - Assert that changing form values triggers StorageManager.saveSettings saves correctly.
    - Assert that clicking popup redirect launches the correct shortcuts url.
  </action>
  <verify>npx vitest run src/options/__tests__/index.test.ts src/popup/__tests__/index.test.ts</verify>
  <acceptance_criteria>
    - npx vitest run passes cleanly with 100% success
  </acceptance_criteria>
  <done>DOM preferences and redirect operations fully tested</done>
</task>

</tasks>

<verification>
- [ ] ts/tsc --noEmit compiles cleanly
- [ ] npx vitest run passes completely across all 7 unit test suites
</verification>

<success_criteria>
- User preferences preserve and restore cleanly across storage layers
- Popup redirects resolve to native chrome shortcuts window
- Vanilla glassmorphic stylesheets apply harmonized adaptive lighting parameters
</success_criteria>

<output>
After completion, create `.planning/phases/04-settings-ui-quick-action-popup/04-01-SUMMARY.md`
</output>
