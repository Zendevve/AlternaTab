---
phase: 03-service-worker-background-wiring
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/background/index.ts
  - src/background/__tests__/index.test.ts
autonomous: true
requirements:
  - FR-001
  - FR-009
  - NFR-006
must_haves:
  truths:
    - "Background worker successfully coordinates command presses and sends messaging packets"
    - "Alarms trigger periodic database pruning loops cleanly"
    - "Restricted URLs checks correctly trigger fallback warning system"
  artifacts:
    - path: "src/background/index.ts"
      provides: "Orchestrator script binding adapters, message bus, and command hooks"
    - path: "src/background/__tests__/index.test.ts"
      provides: "Unit tests verifying background message routing and command safety"
  key_links:
    - from: "src/background/__tests__/index.test.ts"
      to: "src/background/index.ts"
      via: "unit test imports"
      pattern: "import.*"
---

<objective>
Implement the background service worker orchestrator containing the central message routing bus, hourly pruning alarms, restricted page checking guards, and incognito isolation logic, fully unit tested.

Purpose: Event distribution and command mapping background worker.
Output: Service worker index script and complete unit test coverage.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-service-worker-background-wiring/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Background Script Router & Message Bus Coordination</name>
  <files>src/background/index.ts</files>
  <action>
    Create src/background/index.ts implementing core orchestrator class:
    - Instantiate StorageManager, ChromeTabAdapter, and MRUTracker.
    - Wire tab adapters event callbacks (onTabActivated, onTabCreated, onTabRemoved) to drive MRUTracker sequences.
    
    Wire global commands:
    - Listen for `chrome.commands.onCommand` matching `"toggle-switcher"`:
      - Query active browser tab info.
      - Check if the page URL is restricted:
        - Helper returns true for: `chrome://*`, `chrome-extension://*`, and `chromewebstore.google.com`.
        - If restricted: trigger `chrome.notifications.create` rendering a standard notification toast: "Alt+Tab Switcher cannot be opened on restricted browser system pages."
        - If valid: Retrieve standard settings and recent tabs, then send a message `chrome.tabs.sendMessage` action `"toggle-switcher"` into the tab.
        
    Wire background runtime message receivers (`chrome.runtime.onMessage`):
    - `"switch-to-tab"`: triggers adapter `switchToTab(tabId, windowId)`.
    
    Wire hourly alarms:
    - Setup alarm `"mru-cleanup"` running every hour.
    - Upon alert: trigger tracker to perform cleanups across stored sequences.
  </action>
  <verify>npx tsc --noEmit</verify>
  <acceptance_criteria>
    - src/background/index.ts listens to chrome.commands.onCommand
    - Central message router captures switch-to-tab instructions
    - TS builds compiles cleanly without type warnings
  </acceptance_criteria>
  <done>Background index script successfully coordinates command triggers</done>
</task>

<task type="auto">
  <name>Task 2: Background Orchestration Unit Tests</name>
  <files>src/background/__tests__/index.test.ts</files>
  <action>
    Create src/background/__tests__/index.test.ts testing orchestrator interactions:
    - Mock chrome namespaces (commands, alarms, notifications, runtime, tabs) cleanly using standard Vitest mock structures.
    - Test that onTabActivated adapter callback updates the MRU list.
    - Test that command triggers on standard tabs route recent tab payloads accurately to content script.
    - Test that command triggers on restricted URLs invoke the warning notification toast rather than message sends.
    - Test that the central message bus resolves tab switch requests instantly.
  </action>
  <verify>npx vitest run src/background/__tests__/index.test.ts</verify>
  <acceptance_criteria>
    - npx vitest run src/background/__tests__/index.test.ts passes completely with 100% success
  </acceptance_criteria>
  <done>Background worker logic is fully backed by unit tests</done>
</task>

</tasks>

<verification>
- [ ] ts/tsc --noEmit compiles cleanly
- [ ] npx vitest run src/background/__tests__/index.test.ts passes completely
</verification>

<success_criteria>
- Background worker correctly routes active lists to the correct content viewport
- Switcher restrictions are gracefully caught, rendering system visual warnings
- Standard and Incognito lists decouple exactly as specified under split sandboxing
</success_criteria>

<output>
After completion, create `.planning/phases/03-service-worker-background-wiring/03-01-SUMMARY.md`
</output>
