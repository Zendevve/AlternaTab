---
phase: 01-project-setup-core-infrastructure
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - vite.config.ts
  - vitest.config.ts
  - src/manifest.json
  - src/shared/storage.ts
  - src/shared/__tests__/storage.test.ts
autonomous: true
requirements:
  - NFR-003
must_haves:
  truths:
    - "StorageManager successfully stores and retrieves settings and MRU histories"
    - "Build configurations are valid and check without errors"
  artifacts:
    - path: "package.json"
      provides: "Project dependencies and scripts"
    - path: "vite.config.ts"
      provides: "Bundler configuration for browser extension target"
    - path: "src/manifest.json"
      provides: "Manifest V3 declaration"
    - path: "src/shared/storage.ts"
      provides: "Storage abstraction supporting local, sync, session, and in-memory mock modes"
    - path: "src/shared/__tests__/storage.test.ts"
      provides: "Unit tests verifying storage functionality"
  key_links:
    - from: "src/shared/__tests__/storage.test.ts"
      to: "src/shared/storage.ts"
      via: "unit test imports"
      pattern: "import.*StorageManager"
---

<objective>
Scaffold the extension project structure, configure Vite Manifest V3 compilation, and implement the decoupled StorageManager layer with complete unit testing.

Purpose: Establishes a clean type-safe project baseline and guarantees a bulletproof storage persistence layer with zero data loss risk.
Output: Initial boilerplate setup files, `StorageManager` module, and complete unit tests.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-setup-core-infrastructure/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Project Scaffolding & Bundler Setup</name>
  <files>package.json, tsconfig.json, vite.config.ts, vitest.config.ts, src/manifest.json</files>
  <action>
    Create package.json containing standard dependencies:
    - devDependencies: "typescript", "vite", "vite-plugin-web-extension", "vitest", "@types/chrome".
    - scripts: "build", "dev", "test".
    Create vite.config.ts using vite-plugin-web-extension, targeting "src/manifest.json".
    Create vitest.config.ts for fast in-memory test executions.
    Create tsconfig.json configuring modern bundler resolution and JSX options.
    Create src/manifest.json defining Chrome Extension V3 details, registering background worker "src/background/index.ts" (stub), and requesting permissions: "tabs", "storage".
  </action>
  <verify>Run tsc --noEmit (once files are stubbed)</verify>
  <acceptance_criteria>
    - package.json exists and has vite-plugin-web-extension
    - vite.config.ts exists and references src/manifest.json
    - src/manifest.json has manifest_version: 3 and permissions: ["tabs", "storage"]
  </acceptance_criteria>
  <done>Core setup files are created and syntax-checked</done>
</task>

<task type="auto">
  <name>Task 2: StorageManager Abstraction and Unit Tests</name>
  <files>src/shared/storage.ts, src/shared/__tests__/storage.test.ts</files>
  <read_first>.planning/phases/01-project-setup-core-infrastructure/01-CONTEXT.md</read_first>
  <action>
    Define the UserSettings interface:
    - maxVisible: number (default 9, range 3-15)
    - activationMode: "hold" | "toggle" (default "hold")
    - theme: "auto" | "light" | "dark" (default "auto")
    - cardLayout: "grid" | "list" (default "grid")
    - showWindowBadge: boolean (default true)
    
    Define the MRUEntry interface:
    - tabId: number
    - windowId: number
    - lastActive: number
    
    Implement StorageManager class providing static/instance methods:
    - getSettings(): Promise<UserSettings>
    - saveSettings(settings: UserSettings): Promise<void>
    - getMRUHistory(isIncognito: boolean): Promise<MRUEntry[]>
    - saveMRUHistory(history: MRUEntry[], isIncognito: boolean): Promise<void>
    
    Inside storage.ts, use chrome.storage if available:
    - sync storage for UserSettings (key: "user_settings")
    - local storage for regular MRUEntry list (key: "mru_history")
    - session storage for incognito MRUEntry list (key: "incognito_mru_history")
    If chrome.storage is not available (e.g. running in Node for tests), fall back gracefully to a robust in-memory Map so unit tests execute with zero dependencies.
    
    Write src/shared/__tests__/storage.test.ts:
    - Test save and load settings with default values.
    - Test regular history load/save.
    - Test incognito history load/save.
  </action>
  <verify>npx vitest run src/shared/__tests__/storage.test.ts</verify>
  <acceptance_criteria>
    - src/shared/storage.ts contains interfaces UserSettings and MRUEntry
    - src/shared/storage.ts exports StorageManager
    - npx vitest run src/shared/__tests__/storage.test.ts completes with zero errors
  </acceptance_criteria>
  <done>StorageManager persists and restores correctly with unit test coverage</done>
</task>

</tasks>

<verification>
- [ ] ts/tsc --noEmit compiles cleanly
- [ ] npx vitest run src/shared/__tests__/storage.test.ts passes completely
</verification>

<success_criteria>
- All setup configurations are created and compile correctly
- StorageManager works consistently in extension and mock environments
- Sync, local, and session storage scopes are fully isolated and tested
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-setup-core-infrastructure/01-01-SUMMARY.md`
</output>
