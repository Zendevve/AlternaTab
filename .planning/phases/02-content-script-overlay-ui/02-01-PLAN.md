---
phase: 02-content-script-overlay-ui
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content/index.ts
  - src/content/overlay.css
  - src/content/keyhandler.ts
  - src/content/__tests__/keyhandler.test.ts
autonomous: true
requirements:
  - FR-002
  - FR-003
  - FR-005
  - NFR-007
must_haves:
  truths:
    - "Overlay widget attaches securely to a closed Shadow DOM"
    - "Vite compiles content script CSS bundles inline without leaking styles"
    - "KeyHandler correctly tracks selection cycling and hold/toggle mode releases"
  artifacts:
    - path: "src/content/overlay.css"
      provides: "Glassmorphic responsive lists and grids design styling rules"
    - path: "src/content/index.ts"
      provides: "Content script entry initializing Shadow DOM overlay structures"
    - path: "src/content/keyhandler.ts"
      provides: "Key cycling event handler state machine"
    - path: "src/content/__tests__/keyhandler.test.ts"
      provides: "Unit tests verifying keyboard navigation highlights"
  key_links:
    - from: "src/content/__tests__/keyhandler.test.ts"
      to: "src/content/keyhandler.ts"
      via: "unit test imports"
      pattern: "import.*KeyHandler"
---

<objective>
Design and implement the isolated closed Shadow DOM overlay, premium glassmorphism styling sheets, and the Alt-Tab key cycling navigation controller (KeyHandler) complete with Vitest test coverage.

Purpose: Core switcher visual presentation and responsive keyboard input navigation layer.
Output: Content script UI renderer, CSS styles, and KeyHandler state machine.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-content-script-overlay-ui/02-CONTEXT.md
@.planning/phases/02-content-script-overlay-ui/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shadow DOM Setup & Responsive Glassmorphic Overlay Layout</name>
  <files>src/content/index.ts, src/content/overlay.css</files>
  <action>
    Create src/content/overlay.css containing modern aesthetic variables and layouts:
    - Theme variables for Light/Dark modes (glass backgrounds, border highlights, primary accent colors).
    - Centered fixed overlay wrapper using flex or grid.
    - Grid layout using CSS grid: repeat(auto-fill, minmax(130px, 1fr)) with smooth card transitions.
    - List layout: vertical flex rows.
    - Glassmorphic backdrop filter styling: backdrop-filter: blur(12px) saturate(180%).
    - Selection indicator ring (accent border transition) and window badges.
    
    Create src/content/index.ts implementing overlay setup:
    - Attach host element, create closed Shadow DOM container.
    - Import and append compiled overlay.css styles inline (?inline) into the shadow root.
    - Render HTML markup representing card lists dynamically:
      - card elements containing favicons, title strings, truncated domains, active state flags, and window badges.
  </action>
  <verify>Compile and bundle check content scripts via npx tsc --noEmit</verify>
  <acceptance_criteria>
    - src/content/overlay.css exists and declares grid/list glass styles
    - src/content/index.ts mounts a closed Shadow DOM and injects inline styles
  </acceptance_criteria>
  <done>Shadow DOM mounts and renders the glassmorphic card grids correctly</done>
</task>

<task type="auto">
  <name>Task 2: KeyHandler Cycle Navigation State Machine and Tests</name>
  <files>src/content/keyhandler.ts, src/content/__tests__/keyhandler.test.ts</files>
  <read_first>.planning/phases/02-content-script-overlay-ui/02-CONTEXT.md</read_first>
  <action>
    Implement KeyHandler class in src/content/keyhandler.ts:
    - Keeps track of list index selection, list length, hold/toggle mode settings, and key statuses.
    - Handles "keydown" capturing:
      - Alt+Q: increments active index (modulo length).
      - Alt+Shift+Q: decrements active index (modulo length).
      - Arrow keys (Up/Down/Left/Right): standard navigation based on list/grid row structure.
      - Escape: trigger dismiss callback.
      - Enter/Space: trigger switch target callback.
    - Handles "keyup":
      - Releasing "Alt" in Hold Mode triggers instant switch target callback.
    - Handles "blur":
      - Window blur event instantly dismisses the overlay.
      
    Create src/content/__tests__/keyhandler.test.ts:
    - Test forward cycling on Alt+Q.
    - Test backward cycling on Alt+Shift+Q.
    - Test Escape dismisses.
    - Test Enter/Space confirms.
    - Test Hold Mode Alt-release confirms.
    - Test blur event dismisses overlay.
  </action>
  <verify>npx vitest run src/content/__tests__/keyhandler.test.ts</verify>
  <acceptance_criteria>
    - src/content/keyhandler.ts exports KeyHandler class
    - KeyHandler manages active select highlight correctly
    - All tests in keyhandler.test.ts compile and pass successfully
  </acceptance_criteria>
  <done>KeyHandler state machine is complete and fully unit tested</done>
</task>

</tasks>

<verification>
- [ ] ts/tsc --noEmit compiles cleanly
- [ ] npx vitest run src/content/__tests__/keyhandler.test.ts passes completely
</verification>

<success_criteria>
- closed Shadow DOM encapsulates all styles and prevents host contamination
- Alt+Q key cycling navigation triggers highlighted elements and respects boundaries
- Hold and Toggle mode key releases respond accurately under isolated test mocks
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-script-overlay-ui/02-01-SUMMARY.md`
</output>
